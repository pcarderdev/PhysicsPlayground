using DifferentialEquations

function my_dynamics!(du, u, p, t)
    # INPUT:
    # u = current state 
    # p = parameters
    # t = current time

    # OUTPUT:
    # du = how fast each variable is changing right Newton
    
end